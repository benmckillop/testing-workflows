name: Merged

on:
  push:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Increment version number
    if: github.event.pull_request.draft == false
    runs-on: macos-26
    permissions:
      contents: write
      
    steps:
#      - name: Create GitHub release (no suffix at all)
#        uses: benmckillop/Versioning@feature/adding-optional-suffix-to-release-version
#        with:
#          ACTION_TYPE: 'Release'
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Create GitHub release (empty suffix)
        uses: benmckillop/Versioning@feature/adding-optional-suffix-to-release-version
        with:
          ACTION_TYPE: 'Release'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUFFIX: ''
          
      - name: Create GitHub release (empty string suffix)
        uses: benmckillop/Versioning@feature/adding-optional-suffix-to-release-version
        with:
          ACTION_TYPE: 'Release'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUFFIX: ' '
          
      - name: Create GitHub release (suffix)
        uses: benmckillop/Versioning@feature/adding-optional-suffix-to-release-version
        with:
          ACTION_TYPE: 'Release'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUFFIX: 'alpha'
